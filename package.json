{
  "name": "composer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone TypeScript Cloudflare Worker: migrated job-application agent (vector query, company research, resume synthesis).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
